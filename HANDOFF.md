# 引き継ぎメモ

## 現在の目的

2人の操作者が、コントローラーを左右に分けて合計4キャラを操作する仕組みを作成中。
`TwoPlayerScene`への入力・フィールド・4体のPlayer・味方同士の線の接続まで実装済み。

## 実装済み

- `STAGE_TYPE::STAGE_TWO`を追加済み。
- `FieldManager`の`STAGE_TWO`は既存の緑の床モデルを使用。専用の障害物配置分岐は空。
- 床モデル未指定時は空パスの読み込みを省略するガードを追加。
- `TwoPlayerInputSystem`を追加済み。
  - コントローラー0：左側→キャラ0、右側→キャラ1
  - コントローラー1：左側→キャラ2、右側→キャラ3
  - 左側：左スティック移動＋L1発射
  - 右側：右スティック移動＋R1発射
- `TwoPlayerScene`で入力分割を毎フレーム更新し、各Playerに`SetCharacterInput()`で接続。
- `Player`の移動・射撃は分割入力が設定された場合にそれを使用し、通常モードの操作を維持。
- `PlayerManager`に`InputInfo*`を受け取る`AddPlayer()`を追加。
- 4体を四隅に配置（座標はx, y, z）。
  - キャラ0：奥左 `(-17.5, -4.0, 16.0)`、コントローラー0の左側。
  - キャラ1：奥右 `(17.5, -4.0, 16.0)`、コントローラー0の右側。
  - キャラ2：手前左 `(-17.5, -4.0, -9.0)`、コントローラー1の左側。
  - キャラ3：手前右 `(17.5, -4.0, -9.0)`、コントローラー1の右側。
- キャラ0・1がチーム0、キャラ2・3がチーム1。各ペアを味方として設定。
- `STAGE_TWO`ではキャラクターIDに0～3を使用し、選択ステージによる上書きを抑止。
- `BulletManager`を接続し、射撃・描画・衝突後処理・終了処理を追加。
- カメラは位置`(0, 70, -50)`、注視点`(0, 0, 3.5)`、視野角20度。照明も`GameScene`と同じ設定。
- `BBManager`を接続し、各チームの2体を`GameScene`と同じ線で接続。毎フレーム位置を渡して追従させる。
- Spaceキーでタイトルへ戻る処理あり。

## 確認状況

- 最後の線接続追加までDebug/x64ビルド成功。
- 実画面・実機コントローラーによる移動、射撃、線の追従は未確認。
- ビルドコマンド：`./scripts/build.ps1 -Configuration Debug -NoRestore`。
- SDKフォルダーへのアクセス制限で失敗した場合は権限付き実行が必要だった。

## 現在の未コミット変更

このメモ更新直前の`git status --short`は変更なし。今回の更新対象は`HANDOFF.md`のみ。

## 次に行うこと

ユーザーの次の指示を待つ。実機確認する場合は、4体の表示、左右入力の独立動作、L1/R1射撃、味方同士の線の追従、タイトルへ戻って再入場する動作を確認する。

## 注意

- 変更は小さく保ち、必要なファイルだけ確認する。
- ファイルごとに文字コードが異なる。特に`FieldManager.cpp`をShift-JISとして保存するとコメントが文字化けするため注意。
- `PlayerManager.cpp`では編集後の改行混在で構文エラーが発生し、文字コードを変えずCRLFへ統一して解消した。
- ユーザーは既存の大規模なリファクタリングを一度戻しているため、構造変更は事前に範囲を明確にする。
